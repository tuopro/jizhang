# 项目执行规则

开始任务前先阅读 `PREFERENCES.md`，结束前按实际结果更新它；计划不能写成已完成。

- 本项目是独立的月结客户记账小程序，不是报价小程序，也不是 ERP。
- 第一版禁用 AI、大模型和 DeepSeek API；订单文字只允许规则识别。
- 不猜价格，不设通用市场价，不自动创建产品；任何不确定项必须人工确认。
- 产品规格与齿型以 `data/product-specs.js` 为当前权威；同步旧报价项目时只同步产品事实，不复制价格。
- 所有数据访问必须显式带 `tenantId`；正式云端实现还必须从服务端身份校验企业归属。
- 金额用整数分保存；历史发货保存当时产品、数量、单位、单价和金额快照。
- 正式入账、修改、删除、收款和结清均要在服务层校验并保留审计记录。
- UI 以清晰、大按钮、少步骤为先，不加复杂图表、科技感动画或 ERP 式菜单。
- 每个关键功能都要补自动测试；至少运行 `npm test` 和 `npm run check`。
